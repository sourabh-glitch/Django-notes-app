apiVersion: v1
kind: Service
metadata:
  name: {{ include "django-notes-chart.fullname" . }}
  labels:
    {{- include "django-notes-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "django-notes-chart.selectorLabels" . | nindent 4 }}
